# frontend(Streamlit) 실행에 필요한 의존성.
# YOLO/ResNet18 추론은 더 이상 이 프로세스에서 하지 않으므로 ultralytics는
# 필요 없음 — backend/requirements.txt 참고.

streamlit>=1.60  # width= 파라미터(use_container_width 대체) 지원 버전
pandas>=2.0
numpy>=1.24
opencv-python>=4.8
Pillow>=10.0
requests>=2.31
folium>=0.20
streamlit-folium>=0.27

# repair_inpaint.py("복원 예상" 기능)가 이 프로세스 안에서 직접 로드되고
# 있어서 당장은 필요함. 이것도 backend로 옮기면 제거 가능 (추후 과제).
torch>=2.0
torchvision>=0.15

# repair_inpaint.py 상단 안내 참고 — 필요 시 설치
# diffusers
# safetensors
# sentencepiece
# protobuf
