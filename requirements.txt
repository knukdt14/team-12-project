# 모델 학습·분석 전용 의존성 (scripts/*.py, analysis.ipynb).
# 앱 실행(진단/견적/상담 서비스)에 필요한 패키지는 여기 없음 —
# backend/requirements.txt, frontend/requirements.txt를 참고하세요.

ultralytics>=8.3
opencv-python>=4.8
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
seaborn>=0.12
Pillow>=10.0
torch>=2.0
torchvision>=0.15
