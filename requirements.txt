ultralytics>=8.3
opencv-python>=4.8
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
seaborn>=0.12
streamlit>=1.60  # width= 파라미터(use_container_width 대체) 지원 버전
Pillow>=10.0
torch>=2.0
torchvision>=0.15
requests>=2.31
folium>=0.20
streamlit-folium>=0.27

# AI 상담(RAGS.py, Qwen2.5-7B-Instruct) frontend/app.py가 시작할 때부터 바로 import함 — 필수
transformers>=4.40
python-dotenv>=1.0

# frontend/app.py, backend/main.py 실행에 필요한 나머지 패키지는
# frontend/requirements.txt, backend/requirements.txt 참고 (이 파일은 scripts/, analysis.ipynb 등
# 프로젝트 루트 스크립트 재현용 — fastapi/uvicorn 등 서버 실행용 패키지는 각 하위 폴더 참고)
# 손상 복구 예상 이미지 생성(repair_inpaint.py)은 보류 중 — 필요 시 diffusers,
# accelerate, safetensors, sentencepiece, protobuf 별도 설치(repair_inpaint.py 상단 안내 참고)
