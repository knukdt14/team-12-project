fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.0

# 모델 추론 (YOLO 부위 탐지 + ResNet18 손상 종류 분류)
ultralytics>=8.3
opencv-python>=4.8
numpy>=1.24
Pillow>=10.0
torch>=2.0
torchvision>=0.15

# RAG (정비 상담 챗봇) — 벡터스토어 빌드(ai/build_vectorstore.py) + 런타임 검색(services/rag.py) 공용
langchain>=0.3
langchain-community>=0.3
langchain-text-splitters>=0.3
sentence-transformers>=3.0
faiss-cpu>=1.8

# LLM 클라이언트 (services/llm_client.py, Ollama REST 호출)
requests>=2.31

# AI 복원 예상 이미지 (services/image_restorer.py, OpenAI Image API)
openai>=2.0,<3

# 정비소 검색 (routers/repair_shops.py, 카카오 로컬 API)
python-dotenv>=1.0
